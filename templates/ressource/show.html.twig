{% extends 'base.html.twig' %}

{% block title %}Ressource{% endblock %}

{% block body %}
    <section class='container'>
        <h1>Ressource</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ ressource.id }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ ressource.description }}</td>
                </tr>
                <tr>
                    <th>Lien</th>
                    <td>{{ ressource.link }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('ressource_index') }}">retour à la liste</a>

        <a href="{{ path('ressource_edit', {'id': ressource.id}) }}">modifier</a>

        {{ include('ressource/_delete_form.html.twig') }}
    </section>
{% endblock %}
