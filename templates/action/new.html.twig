{% extends 'admin/base.html.twig' %}

{% block title %}New Action
{% endblock %}

{% block body %}
	<section class="container col-auto col-md-9 col-xl-10">
		<div>
			<h1>Create new Action</h1>

			{% if is_granted("ROLE_ADMIN") %}
				{{ include('action/_form.html.twig') }}
			{% endif %}
		</div>

		<a class="btn btn-light mt-1" class="link-dark" href="{{ path('action_index') }}">Retour à la liste</a>
	</section>
{% endblock %}
