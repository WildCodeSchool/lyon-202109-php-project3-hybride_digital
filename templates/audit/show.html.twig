{% extends 'admin/base.html.twig' %}

{% block title %}Audit
{% endblock %}

{% block body %}
	<div class="container col-auto col-md-9 col-xl-10">
		<h1>Visualisation de l'audit</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Identifiant</th>
					<td>{{ audit.id }}</td>
				</tr>
				<tr>
					<th>Nom de l'audit</th>
					<td>{{ audit.name }}</td>
				</tr>
				<tr>
					<th>Date de creation</th>
					<td>{{ audit.datecreation ? audit.datecreation|date('Y-m-d') : '' }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('audit_index') }}">Retour à la liste</a>

		<a href="{{ path('audit_edit', {'id': audit.id}) }}">Modifier</a>

		{{ include('audit/_delete_form.html.twig') }}
	</div>
{% endblock %}
